# 项目规范与指令

## 语言与文档规范
- **语言策略**：本项目所有的文档（包括 `README.md`、`GEMINI.md`、代码注释等）必须使用 **中文** 撰写。
- **注释规范**：复杂的逻辑实现应包含清晰的中文注释。

## 技术规范
- **框架**：Next.js (App Router)。
- **样式**：原生 CSS (CSS Modules)，禁止使用 Tailwind CSS。
- **内容**：使用 MDX 进行文章管理。
- **设计风格**：极简现代风，主色调为中性灰与柔和蓝。

## 协作流程
- **自动编辑**：AI 在执行任务时应保持代码的简洁性与可读性。
- **本地化**：所有 UI 字符串必须为中文。
- **Git 策略**：代码修改后及时进行本地提交（git commit），但 **禁止自动推送（git push）**，除非用户下达明确指令。这是为了避免频繁触发 Vercel 的自动化构建限制。

## 写作规范
- **文件格式**：支持 `.md` (推荐 Typora 用户使用) 和 `.mdx` 格式。
- **元数据 (Frontmatter)**：
  - 推荐在文件开头添加 YAML 元数据（标题、日期、摘要、标签）。
  - 若缺失元数据，系统将自动从第一个一级标题 (`#`) 提取标题，并使用文件创建日期作为发布日期。
- **图片处理**：
  - **重要**：禁止使用本地绝对路径（如 `E:\Documents\...`）。
  - 请将图片存放在 `public/images/` 目录下，并在文档中使用 `/images/文件名.png` 的相对路径引用。
- **内容清洗**：系统会自动移除 HTML 标签中的 `style` 字符串属性（如 Typora 生成的 `style="zoom:150%"`），以确保 MDX 渲染兼容性。
